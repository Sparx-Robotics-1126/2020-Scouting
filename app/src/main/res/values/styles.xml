<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/BText</item>
        <item name="colorPrimaryDark">@color/BBackground</item>
        <item name="colorAccent">@color/BButtonBackground</item>
        <item name="android:titleTextStyle">@style/myTheme.ActionBar.Text</item>
    </style>

    <style name="myTheme.ActionBar.Text" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/BBackground</item>
    </style>

    <style name="J_spinner">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:paddingLeft">15dp</item>
    </style>

    <style name="VerticalLinearLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="t019_basic">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="J_radioButton">
        <item name="android:buttonTint"></item>
        <item name="android:textColor"></item>
    </style>
    <style name="t019_checkBox" parent="t019_basic">
        <item name="android:layout_weight">0</item>
        <item name="android:buttonTint">@color/interact</item>
    </style>
    <style name="t019_radioButton" parent="t019_basic">
        <item name="android:buttonTint">@color/interact</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:paddingTop">5dp</item>
    </style>

    <style name="t019_plusMinusButton" parent="t019_basic">
        <item name="android:layout_width">50dp</item>
        <item name="android:padding">0dp</item>
    </style>

</resources>
